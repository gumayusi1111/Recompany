# 当前环境
NODE_ENV=development

# 后端服务端口
BACKEND_PORT=3002

# 前端服务端口（仅参考，Next.js 内部不使用）
FRONTEND_PORT=3000

# 管理系统端口
MANAGEMENT_PORT=3001

# Next.js 读取的 API 地址（必须 NEXT_PUBLIC_ 前缀）
NEXT_PUBLIC_API_BASE=http://localhost:3002/api

# 数据库连接：指向本地 Docker 容器的 db 服务
DATABASE_URL=postgresql://postgres:postgres@db:5432/company_re

# JWT 密钥（仅用于测试，生产请务必更换）
JWT_SECRET=dev-secret-placeholder

# 跨域配置
CORS_ORIGIN=http://localhost:3000